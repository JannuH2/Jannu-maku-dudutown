# Locally patched: the CurseForge release's citadel.mixins.json references a
# citadel.refmap.json that isn't actually bundled in the jar, which crashes the
# game on init (client.LevelRendererMixin injection fails with no refmap loaded).
# Removed that one mixin entry (team-color render redirect) and repackaged;
# everything else in the jar is untouched. No newer CurseForge release fixes this.
name = "Citadel (Fabric)"
filename = "citadelfabric-1.21.1-1.2.0.jar"
side = "both"

[download]
url = "https://jannuh2.github.io/Jannu-maku-dudutown/mods/citadelfabric-1.21.1-1.2.0.jar"
hash-format = "sha256"
hash = "66ea1f8f17b254ea484a4a3ecf469f6062af662854450ca038ea55c43b76dd2e"
